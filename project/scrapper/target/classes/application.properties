server.port=8081

app.test = 1123

springdoc.swagger-ui.path=/swagger-ui

app.scheduler.interval=10000

db.postgresql.url = ${POSTGRES_URL}

db.postgresql.username = pgpusr

db.postgresql.password = pgppwd
